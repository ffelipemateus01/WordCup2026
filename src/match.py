from pygame import Surface, Rect
from pygame.font import Font

class Match:
    def __init__(self, window, mode: str):
        self.window = window
        self.mode = mode

    def run(self):
        pass